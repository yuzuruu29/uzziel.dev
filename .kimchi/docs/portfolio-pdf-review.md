# Portfolio PDF Review

## Verdict: NEEDS_FIXES

The generated PDF file is structurally valid and has the expected metadata, but the content spot-check (verifying that all required project titles appear in the PDF) could not be completed because no PDF text-extraction tools are available in this environment and the PDF's text streams use glyph-encoded embedded fonts rather than plain text.

## Verification Output

### File Existence and Basic Properties

Commands run:

```bash
cd /mnt/c/Portfolio
ls -lh Uzziel_Malolos_Portfolio.pdf
pdfinfo Uzziel_Malolos_Portfolio.pdf 2>/dev/null || echo "pdfinfo not available"
```

Output:

```text
-rwxrwxwxwx 1 uzzie uzzie 708K Jun 29 14:54 Uzziel_Malolos_Portfolio.pdf
pdfinfo not available
```

- File exists: yes
- Path: `/mnt/c/Portfolio/Uzziel_Malolos_Portfolio.pdf`
- Size: 724,064 bytes (708K)
- Creation/Mod date: 2026-06-29 06:54:31 UTC

### PDF Validity and Metadata

Direct inspection of the file header and document info dictionary confirms:

- PDF version: `PDF-1.4`
- Title: `Uzziel Malolos – Portfolio`
- Producer: `Skia/PDF m149` (generated by Chrome Headless)
- Creator: `Mozilla/5.0 ... HeadlessChrome/149.0.0.0 ...`
- The file contains valid `startxref`, `%%EOF`, and cross-reference structure.

### Page Count

Because `pdfinfo` and PDF libraries were unavailable, page count was determined by scanning the PDF object tree for `/Type /Page` objects.

Command:

```bash
python3 -c "import re; data=open('Uzziel_Malolos_Portfolio.pdf','rb').read(); print(len(re.findall(rb'/Type\\s*/Page(?!=s)', data)))"
```

Result: **28 pages**

### Text Extraction Attempts

The following tools/methods were attempted and found unavailable:

- `pdfinfo`
- `pdftotext`
- `PyPDF2`
- `pypdf`
- `pdfplumber`
- `pip3` / `python3 -m pip` (could not install a library)

Manual decompression of the `FlateDecode` content streams was performed with Python `zlib`. The decompressed streams contain PDF graphics operators such as:

```text
/F4 11.3299999 Tf
1 0 0 -1 0 12 Tm
<54> Tj
8.5961304 0 Td <42> Tj
...
```

The strings inside `<...>` are glyph IDs, not Unicode text. The PDF uses embedded fonts with `/ToUnicode` CMaps, but no local tool is present to map those glyphs back to characters. Running `strings` on the raw or decompressed PDF did not yield any of the expected project titles.

## Content Spot-Check Results

The following project titles were requested to be verified in the PDF text:

- TIRADOR
- Heart and Soil
- HiveMind
- Yusel AI
- AI Micro-Agency
- The Lord's Worship Center
- Pomodoro App
- Saklay Charity Website
- Land Conversion
- Oka's Farm Bookkeeping
- Willow Creek Farmstay
- ClearPath Lawn Care
- Harvest Table Kitchen

Result: **Could not verify any of the titles.**

## Issues

1. **Content verification blocked by environment / PDF encoding**
   - File: `/mnt/c/Portfolio/Uzziel_Malolos_Portfolio.pdf`
   - Description: The PDF is valid and has the correct title metadata, but the 13 required project titles could not be confirmed because the environment lacks PDF text-extraction tools and the PDF stores text as glyph IDs in embedded fonts.
   - Suggested fix: Re-run verification in an environment with `pdftotext`, `PyPDF2`, or `pypdf` installed, or generate/provide a text-accessible version of the PDF. A one-liner such as `pdftotext Uzziel_Malolos_Portfolio.pdf - | grep -F "TIRADOR"` should then succeed for each project title.

## Summary

- PDF is structurally valid: yes
- PDF size and page count: 708K, 28 pages
- PDF metadata title matches expected portfolio: yes
- Required project titles confirmed inside PDF: no (verification blocked)
